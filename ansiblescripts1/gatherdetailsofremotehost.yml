---
-  name: Gather details
   hosts: all
   gather_facts: 'yes'
   tasks:
     -  name: Print Hostname
        debug:
          msg: 'Hostname: {{ ansible_hostname }}'
     -  name: Print OS Distribution
        debug:
          msg: 'OS Distribution: {{ ansible_distribution }}'
